apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whoami-api
  namespace: demo
spec:
  chart:
    spec:
      version: "2026.909.737"
  values:
    config:
      environment: staging
