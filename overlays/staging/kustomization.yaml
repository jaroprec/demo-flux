apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: staging

resources:
  - ../../infrastructure

# Chart versions and environment values for this cluster.
patches:
  - path: kube-prometheus-stack.yaml
  - path: whoami-api.yaml
  - path: nginx-web.yaml
